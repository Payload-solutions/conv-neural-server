version: '3.7'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    networks:
      - default
    volumes:
      - ".:/app"
  bot:
    build: ./ArthurBot
    depends_on:
      - web
    networks:
      - default

networks:
  default: